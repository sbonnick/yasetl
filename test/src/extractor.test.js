const jiraParser = require('../../src/extractor');
const expect     = require('chai').expect;
const sinon      = require('sinon');
const test       = require('sinon-test')(sinon);

describe('Extractor', () => {
  describe('run()', () => {

    it.skip('One off extraction', test(function() {

    }))

    it.skip('Cron scheduled extraction', test(function() {

    }))
  })
})